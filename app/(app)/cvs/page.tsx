import { CvManager } from "@/components/cv/cv-manager"; import { createClient } from "@/lib/supabase/server";
export default async function CVsPage() { const supabase = await createClient(); const { data } = await supabase.from("cv_profiles").select("*").order("created_at", { ascending: false }); return <div className="page"><div className="mb-7"><p className="text-sm font-medium text-indigo-600">Your foundation</p><h1 className="mt-1 text-2xl font-semibold">CV profiles</h1><p className="mt-2 text-sm text-slate-500">Keep a focused version for each career direction. Applied selects the best starting point per role.</p></div><CvManager initial={data || []} /></div>; }
