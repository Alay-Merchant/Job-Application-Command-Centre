import Link from "next/link"; import { type LucideIcon } from "lucide-react"; import { Button } from "@/components/ui/button";
export function EmptyState({ icon: Icon, title, body, href, action }: { icon: LucideIcon; title: string; body: string; href: string; action: string }) { return <div className="rounded-xl border border-dashed bg-white px-6 py-12 text-center dark:bg-slate-900"><Icon className="mx-auto size-8 text-indigo-600" /><h2 className="mt-4 font-semibold">{title}</h2><p className="mx-auto mt-2 max-w-md text-sm text-slate-500">{body}</p><Button asChild className="mt-5"><Link href={href}>{action}</Link></Button></div>; }
