import { NextResponse } from "next/server"; import { z } from "zod"; import { requireUser, apiError } from "@/lib/auth";
const schema = z.object({ applicationId: z.string().uuid().nullable().optional(), title: z.string().max(120).optional() });
export async function POST(request: Request) { const auth = await requireUser(); if ("error" in auth) return auth.error; try { const body = schema.parse(await request.json()); if (body.applicationId) { const { data } = await auth.supabase.from("applications").select("id").eq("id", body.applicationId).eq("user_id", auth.user.id).single(); if (!data) throw new Error("Application not found."); } const { data, error } = await auth.supabase.from("coach_threads").insert({ user_id: auth.user.id, application_id: body.applicationId || null, title: body.title || "New coaching conversation" }).select().single(); if (error) throw error; return NextResponse.json({ thread: data }); } catch (error) { return apiError(error); } }
