import { notFound } from "next/navigation"; import { createClient } from "@/lib/supabase/server"; import { ApplicationDetail } from "@/components/kit/application-detail";
export default async function ApplicationPage({ params }: { params: { id: string } }) { const supabase = await createClient(); const [{ data: application }, { data: cvs }] = await Promise.all([supabase.from("applications").select("*,job:jobs(*),cv:cv_profiles(*)").eq("id", params.id).single(), supabase.from("cv_profiles").select("id,label").order("label")]); if (!application) notFound(); const { data: kit } = await supabase.from("application_kits").select("*").eq("application_id", application.id).maybeSingle(); return <div className="page"><ApplicationDetail initialApplication={application as never} initialKit={kit} cvs={cvs || []} /></div>; }
