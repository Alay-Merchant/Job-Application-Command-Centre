"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { BarChart3, BriefcaseBusiness, Building2, ClipboardList, LayoutDashboard, MessageCircle, Search, Settings, Sparkles } from "lucide-react";
import { cn } from "@/lib/utils";
const links = [{ href: "/dashboard", label: "Dashboard", icon: LayoutDashboard }, { href: "/cvs", label: "CV Profiles", icon: ClipboardList }, { href: "/jobs", label: "Find jobs", icon: Search }, { href: "/applications", label: "Applications", icon: BriefcaseBusiness }, { href: "/companies", label: "Companies", icon: Building2 }, { href: "/coach", label: "Coach", icon: MessageCircle }, { href: "/insights", label: "Insights", icon: BarChart3 }, { href: "/settings", label: "Settings", icon: Settings }];
export function Sidebar({ mobile = false }: { mobile?: boolean }) { const pathname = usePathname(); return <aside className={cn("w-60 shrink-0 border-r bg-white px-3 py-5 dark:bg-slate-950 flex flex-col", mobile ? "h-full" : "hidden md:flex")}><Link href="/dashboard" className="mb-8 flex items-center gap-2 px-3 text-lg font-semibold"><span className="grid size-8 place-items-center rounded-lg bg-indigo-600 text-white"><Sparkles className="size-4" /></span>Applied</Link><nav className="space-y-1" aria-label="Main navigation">{links.map(({ href, label, icon: Icon }) => { const active = pathname === href || (href !== "/dashboard" && pathname.startsWith(`${href}/`)); return <Link key={href} href={href} aria-current={active ? "page" : undefined} className={cn("flex items-center gap-3 rounded-lg px-3 py-2 text-sm font-medium text-slate-600 hover:bg-slate-100 dark:text-slate-400 dark:hover:bg-slate-900", active ? "bg-indigo-50 text-indigo-700 dark:bg-indigo-950/40 dark:text-indigo-300" : "")}><Icon className="size-4" />{label}</Link>; })}</nav><p className="mt-auto px-3 text-sm text-slate-500">Built for thoughtful applications.</p></aside>; }
