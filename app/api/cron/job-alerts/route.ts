import { NextResponse } from "next/server"; import { admin } from "@/lib/supabase/admin"; import { searchJobs, rankJobs } from "@/lib/adzuna"; import { sendJobAlerts } from "@/lib/resend";
export const runtime = "nodejs";
export async function GET(request: Request) { if (!process.env.CRON_SECRET || request.headers.get("authorization") !== `Bearer ${process.env.CRON_SECRET}`) return NextResponse.json({ error: "Unauthorised" }, { status: 401 }); const { data: searches, error } = await admin.from("saved_searches").select("*,cv:cv_profiles(structured)").eq("active", true); if (error) return NextResponse.json({ error: error.message }, { status: 500 }); let sent = 0; for (const search of searches || []) { try { const { data: profile } = await admin.from("profiles").select("preferences").eq("id", search.user_id).maybeSingle(); if (profile?.preferences?.job_alert_emails === false) continue; const jobs = await searchJobs({ what: search.query || "", where: search.where_location || "London", salaryMin: search.min_salary || undefined }); const keywords = search.cv?.structured?.keywords || search.cv?.structured?.skills || []; const top = rankJobs(jobs, keywords).slice(0, 5); const { data: user } = await admin.auth.admin.getUserById(search.user_id); if (user?.user) { await sendJobAlerts(user.user, top); sent++; } } catch { /* One failed external search must not block the remaining alerts. */ } } return NextResponse.json({ ok: true, sent }); }
