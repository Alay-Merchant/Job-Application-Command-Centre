import OpenAI from "openai"; import { z } from "zod"; import { requireUser, apiError } from "@/lib/auth"; import { coachSystem } from "@/lib/prompts"; import { allowAiRequest } from "@/lib/rate-limit";
export const runtime = "nodejs"; const schema = z.object({ threadId: z.string().uuid(), message: z.string().min(1).max(5000) });
export async function POST(request: Request) { const auth = await requireUser(); if ("error" in auth) return auth.error; try { const { threadId, message } = schema.parse(await request.json()); const { data: thread } = await auth.supabase.from("coach_threads").select("*,application:applications(job:jobs(*),cv:cv_profiles(*))").eq("id", threadId).eq("user_id", auth.user.id).single(); if (!thread) throw new Error("Conversation not found."); if (!allowAiRequest(auth.user.id, 40)) return new Response("Daily AI generation limit reached.", { status: 429 }); const { data: messages } = await auth.supabase.from("coach_messages").select("role,content").eq("thread_id", threadId).eq("user_id", auth.user.id).order("created_at").limit(14); await auth.supabase.from("coach_messages").insert({ user_id: auth.user.id, thread_id: threadId, role: "user", content: message }); if (!process.env.OPENAI_API_KEY) throw new Error("OPENAI_API_KEY is not configured."); const app = thread.application as { job?: unknown; cv?: { structured?: unknown } } | null; const context = app ? `\nApplication context: CV ${JSON.stringify(app.cv?.structured || {})}; job ${JSON.stringify(app.job || {})}` : ""; const stream = await new OpenAI({ apiKey: process.env.OPENAI_API_KEY }).chat.completions.create({ model: "gpt-4o", stream: true, messages: [{ role: "system", content: coachSystem + context }, ...(messages || []).map((m) => ({ role: m.role as "user" | "assistant", content: m.content })), { role: "user", content: message }] }); let result = ""; const encoder = new TextEncoder(); const readable = new ReadableStream({ async start(controller) { try { for await (const chunk of stream) { const text = chunk.choices[0]?.delta?.content || ""; result += text; controller.enqueue(encoder.encode(text)); } await auth.supabase.from("coach_messages").insert({ user_id: auth.user.id, thread_id: threadId, role: "assistant", content: result }); controller.close(); } catch (error) { controller.error(error); } } }); return new Response(readable, { headers: { "Content-Type": "text/plain; charset=utf-8", "Cache-Control": "no-cache" } }); } catch (error) { return apiError(error); } }
